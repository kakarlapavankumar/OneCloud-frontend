export const APP_NAME = "OneCloud";

export const STORAGE_KEYS = {
  employees: "onecloud_employees",
  attendance: "onecloud_attendance",
  leaves: "onecloud_leaves",
  user: "onecloud_user",
  authenticated: "onecloud_authenticated",
} as const;
